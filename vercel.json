{
  "buildCommand": "cd ../.. && pnpm --filter @workspace/terminal run build",
  "outputDirectory": "artifacts/terminal/dist",
  "framework": null,
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
